FILE(GLOB SOURCES . *.cpp)
FILE(GLOB UIS . *.ui)
QT4_WRAP_UI(GENUI ${UIS})
QT4_WRAP_CPP(MOCS main.h GlutWidget.h)
INCLUDE_DIRECTORIES(${QT_INCLUDES})
ADD_EXECUTABLE(qtTest ${SOURCES} ${MOCS} ${GENUI})
TARGET_LINK_LIBRARIES(qtTest ${QT_LIBRARIES})
