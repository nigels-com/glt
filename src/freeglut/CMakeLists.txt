FILE(GLOB SOURCES . *.c)
IF(WIN32)
ELSE(WIN32)
  ADD_DEFINITIONS(-DHAVE_SYS_TYPES_H)
  ADD_DEFINITIONS(-DHAVE_SYS_IOCTL_H)
  ADD_DEFINITIONS(-DHAVE_FCNTL_H)
ENDIF(WIN32)
ADD_LIBRARY(freeglut ${SOURCES})
